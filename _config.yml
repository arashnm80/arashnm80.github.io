# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Arash Nemat Zadeh
email: Arashnm80@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  teacher & programmer
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://Arashnm80.com" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: Arashnm80
github_username:  Arashnm80

# Build settings
theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap # https://github.com/jekyll/jekyll-sitemap
  - jekyll-paginate # https://jekyllrb.com/docs/pagination/
  - jekyll-git_metadata # for using {{ site.github.build_date }}

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# my modifications:
permalink: pretty # to remove .html from the end
show_excerpts: true
# excerpt_separator: "<!--more-->" # global excerpt separator for the blog posts
timezone: Asia/Tehran
google_analytics: G-C32LDMX65L
header_pages:
  - works.md
  - posts.md
  - donate.md
  - handbook/handbook.md
paginate: 5
paginate_path: "/posts/page:num/"
defaults:
  - scope:
      path: "handbook" # Adjust the path to your folder
      type: "pages"               # The type can be "pages" or "posts"
    values:
      layout: "page"              # The layout you want to apply
exclude:
  - _old_files